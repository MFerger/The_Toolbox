// express-generator global install
$ npm install express-generator -g

//express handlebars and git
$ express --hbs --git .
$ npm install
$ nodemon

// express-generator with knex
$ express --hbs --git .
$ npm install
$ npm install --save pg knex@0.9.0
$ nodemon
